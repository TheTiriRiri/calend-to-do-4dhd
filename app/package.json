{
  "name": "app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.app.json && tsc -p tsconfig.node.json",
    "test:unit": "vitest run",
    "test:e2e": "playwright test",
    "test:e2e:docker": "docker run --rm --network host -v \"$(cd .. && pwd)\":/work -w /work/app -e E2E_WEBKIT=1 -e HOME=/tmp --user \"$(id -u):$(id -g)\" mcr.microsoft.com/playwright:v1.62.1-noble bash -c \"npm ci && npm run test:e2e\"",
    "deploy": "npm run build && wrangler pages deploy dist --project-name cal-to-do-kk --branch main --commit-dirty=true"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "@tsconfig/svelte": "^5.0.8",
    "@types/node": "^24.13.3",
    "svelte": "^5.56.10",
    "svelte-check": "^4.7.6",
    "typescript": "~6.0.2",
    "vite": "^8.2.2",
    "vite-plugin-pwa": "^1.3.0",
    "vitest": "^4.1.11",
    "wrangler": "^4.129.0"
  },
  "dependencies": {
    "dexie": "^4.4.5"
  }
}
